const assert = require('chai').assert;

const testLocalUser = {
    username: '12345',
    password: '98765',
    email: 'fb@fb.com',
    name: 'Devon'
}

describe('Test facebook user username testing', function () {
    it('Should test the username of Test comment data type', function () {
        assert.equal(testLocalUser.username.length, 5);
    });
    it('Should test the username of Test comment data type', function () {
        assert.equal(testLocalUser.username, '12345');
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isString(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.username);
    });
    it('Should test the username of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.username);
    });
});

describe('Test facebook user email testing', function () {
    it('Should test the email of Test comment data type', function () {
        assert.equal(testLocalUser.email.length, 9);
    });
    it('Should test the email of Test comment data type', function () {
        assert.equal(testLocalUser.email, 'fb@fb.com');
    });
    it('Should test the email of Test comment data type', function () {
        assert.notProperty(testLocalUser.email, 'fb@fb.com');
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isString(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.email);
    });
});

describe('Test facebook user password testing', function () {
    it('Should test the password of Test comment data type', function () {
        assert.equal(testLocalUser.password.length, 5);
    });
    it('Should test the password of Test comment data type', function () {
        assert.equal(testLocalUser.password, '98765');
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isString(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.password);
    });
    it('Should test the password of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.password);
    });
});

describe('Test facebook user name testing', function () {
    it('Should test the name of Test comment data type', function () {
        assert.equal(testLocalUser.name.length, 5);
    });
    it('Should test the name of Test comment data type', function () {
        assert.equal(testLocalUser.name, 'Devon');
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isString(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotBoolean(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNull(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testLocalUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNaN(testLocalUser.name);
    });
});