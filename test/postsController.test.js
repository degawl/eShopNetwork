const assert = require('chai').assert;
const comment = require('../controllers/comments');

describe('Comment Sanitization', function () {
    it('Comment controller should return sanitized string');
    assert.equal(create(), )
});