const assert = require('chai').assert;


const testFacebookUser = {
    id: '12345',
    token: '98765',
    email: 'fb@fb.com',
    name: 'Devon'
}

describe('Test facebook user id testing', function () {
    it('Should test the id of Test comment data type', function () {
        assert.equal(testFacebookUser.id.length, 5);
    });
    it('Should test the id of Test comment data type', function () {
        assert.equal(testFacebookUser.id, '12345');
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isString(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.id);
    });
    it('Should test the id of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.id);
    });
});

describe('Test facebook user email testing', function () {
    it('Should test the email of Test comment data type', function () {
        assert.equal(testFacebookUser.email.length, 9);
    });
    it('Should test the email of Test comment data type', function () {
        assert.equal(testFacebookUser.email, 'fb@fb.com');
    });
    it('Should test the email of Test comment data type', function () {
        assert.notProperty(testFacebookUser.email, 'fb@fb.com');
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isString(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.email);
    });
    it('Should test the email of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.email);
    });
});

describe('Test facebook user token testing', function () {
    it('Should test the token of Test comment data type', function () {
        assert.equal(testFacebookUser.token.length, 5);
    });
    it('Should test the token of Test comment data type', function () {
        assert.equal(testFacebookUser.token, '98765');
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isString(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.token);
    });
    it('Should test the token of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.token);
    });
});

describe('Test facebook user name testing', function () {
    it('Should test the name of Test comment data type', function () {
        assert.equal(testFacebookUser.name.length, 5);
    });
    it('Should test the name of Test comment data type', function () {
        assert.equal(testFacebookUser.name, 'Devon');
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isString(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotBoolean(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNull(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotArray(testFacebookUser.name);
    });
    it('Should test the name of Test comment datatype', function () {
        assert.isNotNaN(testFacebookUser.name);
    });
});